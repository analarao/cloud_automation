apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.csModel.deployment.name }}
  namespace: {{ .Release.Namespace }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cs-model
  template:
    metadata:
      labels:
        app: cs-model
    spec:
      containers:
      - name: cs-model
        # Image is built from values.yaml
        image: "{{ .Values.csModel.image}}"
        imagePullPolicy: IfNotPresent
        command: ["python", "cs_model_service.py"]
        ports:
        - containerPort: 9001
          name: metrics
          protocol: TCP
        envFrom:
        - configMapRef:
            # References the name of the ConfigMap created above
            name: {{ .Release.Name }}-service-urls
