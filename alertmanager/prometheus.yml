# Path: grafana/provisioning/datasources/prometheus.yml

apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://prometheus:9090  # <-- THIS IS THE KEY FIX
    isDefault: true
    version: 1
    editable: false