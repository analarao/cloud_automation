apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-python-app
  namespace: {{ .Release.Namespace }}
  labels:
    app: {{ .Release.Name }}-python-app
spec:
  type: ClusterIP
  selector:
    app: {{ .Release.Name }}-python-app
  ports:
  - port: 9001
    targetPort: 9001
    protocol: TCP
    name: metrics
